services:
  programming-brain:
    build: .
    container_name: programming-brain
    restart: unless-stopped
    ports:
      - "1001:1001"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:1001/"]
      interval: 30s
      timeout: 5s
      retries: 3
